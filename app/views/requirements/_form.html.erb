<%= simple_form_for(@requirement) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :requirement %>
    <%= f.association :job %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
